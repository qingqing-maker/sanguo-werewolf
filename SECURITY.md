# 安全边界

## 绝不能提交到 GitHub

- `.env`、`.env.*` 中的真实 API Key、访问令牌和生产配置。
- `runs/` 下的 LLM/TTS 预算账本与锁文件。
- `game-logs/` 下的对局事件日志。
- `audio-cache/` 与本地生成文件。
- 浏览器 session token、云平台访问令牌、个人编辑器或 Agent 配置。

仓库只提交 `.env.example` 或 `.env.production.example` 这类不含真实值的模板。真实 Secret 只能放在云平台的加密环境变量中。

## 当前单实例信任模型

- 每个浏览器生成 256-bit 随机会话令牌；服务端只保存 SHA-256 摘要。
- 云端同一时间只允许一个房间。没有房间时，任意已认证访客可以创建房间。
- 房间创建者自动成为主持人；其他访客只能观战，不能开始、暂停、取消、修改设置。
- 房间和会话仅存在于当前 Node 进程中；重启后房间消失，需要重新创建。
- LLM/TTS Key 永远只在服务端使用，不发送到浏览器。
- 匿名状态接口只返回受限观众投影；设置读取与写入均要求主持人会话。

这不是账号系统，也不适合互不信任的大规模公开社区。公开部署时，应同时启用云平台 DDoS/流量保护、严格预算和账单告警。

## 泄露处理

如果 Key 曾进入 Git 历史、Issue、日志、截图或公开聊天：

1. 立即在对应云平台禁用或轮换旧 Key。
2. 更新云端 Secret，不要只删除工作区里的明文。
3. 检查调用明细和账单。
4. 使用 GitHub Secret Scanning / Push Protection 阻止再次提交。

## 报告问题

请通过私有渠道报告安全问题，不要在公开 Issue 中附带密钥、完整请求头、账单截图或用户日志。
